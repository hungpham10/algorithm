
[project]
name = "workflow"
version = "0.1.0"
description = "My workflow project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "prefect",
    "requests>=2.33.1",
    "curl_cffi>=0.7.4",
]

[tool.uv]
managed = true
package = false
